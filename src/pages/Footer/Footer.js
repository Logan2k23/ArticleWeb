import "./Footer.css"

const Footer=()=>{
    return(
        <>
            <div className="foot">
            <h3>FOOTER</h3>
            <para>Since I have always loved oats, I wanted to answer this.

This will show the conversion from weight in grams(g) to the volume in tablespoons (tbsp) using the reference of a weight in grams equivalent to a volume in dry cups from a nutrition data website that provides nutrition facts about almost every food in production in the U.S. First, a reference of the nutritional data:</para>
            </div>
        </>
    )
    
}

export default  Footer;